[package]
name = "petrivet"
version = "0.1.0"
edition = "2024"
description = "A Rust library for working with Petri nets"
repository = "https://github.com/MichaelOwenDyer/petrivet"
keywords = ["petrinets", "modeling", "analysis"]

[dependencies]
good_lp = { version = "1.15.0", default-features = false, features = ["microlp"] }
petgraph = "0.8.2"
quick-xml = { version = "0.39.2", features = ["serialize"], optional = true }
serde = { version = "1.0.228", features = ["derive"], optional = true }

[features]
default = ["pnml"]
pnml = ["serde", "quick-xml"]
